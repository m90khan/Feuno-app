nav.navigation
  .navigation__wrapper
    a.navigation__link(href="/") Feuno
    svg.navigation__link__icon
    ul.navigation__list 
      each item in navigation.data.list
        li.navigation__list__item 
          a.navigation__list__link(href=ctx.handleLinkResolver(item.link))=item.text